---

---

<section class="intro-section">
  <div class="intro-text">
    <p class="lead-paragraph">
      I explore the world of automation and practical AI, helping developers and
      curious minds achieve more with less.
    </p>
  </div>

  <div class="what-i-do">
    <h2 class="section-title">What I do</h2>
    <ul class="activities-list">
      <li class="activity-item">
        Building and scaling automation workflows with tools like n8n.
      </li>
      <li class="activity-item">
        Applying practical AI and Python to solve real-world problems.
      </li>
      <li class="activity-item">
        Creating educational content on YouTube to share what I learn.
      </li>
    </ul>
  </div>
</section>

<style>
  .intro-section {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    padding: 2rem 0;
  }

  .intro-text {
    display: flex;
    flex-direction: column;
  }

  .lead-paragraph {
    color: var(--primary-text);
    font-size: 1rem;
    line-height: 1.5;
    font-family: var(--font-family-sans);
    margin: 0;
    font-weight: 400;
  }

  .what-i-do {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .section-title {
    color: var(--primary-text);
    font-size: 1.125rem;
    font-weight: 600;
    margin: 0;
    line-height: 1.3;
    font-family: var(--font-family-sans);
  }

  .activities-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .activity-item {
    color: var(--secondary-text);
    font-size: 0.875rem;
    line-height: 1.4;
    font-family: var(--font-family-sans);
    position: relative;
    padding-left: 1rem;
  }

  .activity-item::before {
    content: "•";
    color: var(--primary-text);
    position: absolute;
    left: 0;
    font-size: 0.875rem;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .intro-section {
      gap: 1rem;
      padding: 1rem 0;
    }

    .lead-paragraph {
      font-size: 0.95rem;
    }

    .section-title {
      font-size: 1rem;
    }

    .activity-item {
      font-size: 0.8rem;
      line-height: 1.3;
    }
  }
</style>
