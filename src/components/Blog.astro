---

---

<section class="blog-section">
  <h2 class="blog-title">From the Blog</h2>
  <div class="blog-placeholder">
    <p class="coming-soon">Coming soon...</p>
    <p class="blog-description">
      I'll be sharing tutorials on no-code automation with tools like n8n and
      APIs, alongside practical Python tips and AI tricks. Join me in learning
      how to work smarter, not harder.
    </p>
  </div>
</section>

<style>
  .blog-section {
    padding: 1rem 0;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .blog-title {
    color: var(--primary-text);
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.3;
    font-family: var(--font-family-sans);
    letter-spacing: -0.02em;
  }

  .blog-placeholder {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1.5rem;
    border: 1px solid var(--border-color);
    border-radius: 0.5rem;
    background: rgba(255, 255, 255, 0.02);
  }

  .coming-soon {
    color: var(--secondary-text);
    font-size: 1rem;
    font-weight: 500;
    margin: 0;
    line-height: 1.5;
    font-family: var(--font-family-sans);
  }

  .blog-description {
    color: var(--secondary-text);
    font-size: 0.875rem;
    margin: 0;
    line-height: 1.6;
    font-family: var(--font-family-sans);
    opacity: 0.8;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .blog-section {
      padding: 1rem 0;
    }

    .blog-title {
      font-size: 1.375rem;
    }

    .blog-placeholder {
      padding: 1.25rem;
    }

    .coming-soon {
      font-size: 0.95rem;
    }

    .blog-description {
      font-size: 0.8rem;
    }
  }
</style>
